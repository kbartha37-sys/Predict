package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Vibrant Palette - High Fidelity Light Theme
val DarkBg = Color(0xFFF3F4F9)        // Light page background (#F3F4F9)
val DarkCard = Color(0xFFFFFFFF)      // Pure white card surfaces
val SecondaryCard = Color(0xFFEEF2F6) // Warm light gray-blue for borders/tabs/secondary rows
val SportsGreen = Color(0xFF10B981)    // Vibrant emerald green (#10B981) for success / active indicators
val SportsGreenMuted = Color(0xFFE0E7FF) // Indigo light background background (#E0E7FF) for active badges / chips
val SportsBlue = Color(0xFF4F46E5)     // Indigo-600 Brand Accent (#4F46E5)
val WarningOrange = Color(0xFFF59E0B)   // Ambition yellow-orange
val ErrorRed = Color(0xFFEF4444)       // Alerts / red cards
val CardOutline = Color(0xFFEEF2F6)    // Soft indigo-gray border (#EEF2F6)
val TextPrimary = Color(0xFF0F172A)    // Elegant Slate-900 high contrast black
val TextSecondary = Color(0xFF475569)  // Slate-600 subtitles / secondary labels
val LiveDotRed = Color(0xFFEF4444)     // Crimson active live pulse indicator

// Special Predictor Probability Bar Colors
val ProbHomeColor = Color(0xFF6366F1)  // Indigo-500 (#6366F1)
val ProbDrawColor = Color(0xFF94A3B8)  // Slate-400 (#94A3B8)
val ProbAwayColor = Color(0xFF38BDF8)  // Sky-400 (#38BDF8)
